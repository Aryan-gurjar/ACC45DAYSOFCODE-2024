#include <stdio.h>

int main() {
    int X;
    printf("Enter the total points scored by the team: ");
    scanf("%d", &X);

    if (X >= 12) {
        printf("Yes\n");
    } else {
        printf("No\n");
    }

    return 0;
}
